from Game.board import Board


def possible_wins(game: Board) -> float:
    """
    Checks the possible wins of each player and does player - opponent and returns
    :param game: The game state
    :return: a float evaluating the position
    """
    if game.get_turn:
        pass
    else:
        pass

    return

def weighted_possible_wins(game: Board) -> float:
    """
    Works similar to before, but with weights for how close a win is
    :param game: The game state
    :return: A float evaluating the position
    """
    pass